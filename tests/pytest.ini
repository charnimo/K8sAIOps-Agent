[pytest]
markers =
    unit: Pure unit tests — no cluster required
    integration: Integration tests — require a running Kubernetes cluster
    slow: Tests that take more than a few seconds (scale operations, etc.)

testpaths = tests
addopts = -v --tb=short