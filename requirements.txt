# AIOps Agent — Python dependencies

# Kubernetes client
kubernetes>=29.0.0

# LangChain (AI agent layer — used in agent/ module)
langchain>=0.2.0
langchain-openai>=0.1.0      # or langchain-anthropic, etc.
langchain-community>=0.2.0

# Web backend (FastAPI)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0

# Logging / observability
structlog>=24.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0


sqlalchemy>=2.0.0
bcrypt>=4.1.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9
